# Production environment variables (Render)
# REQUIRED: paste your MongoDB Atlas connection string
MONGO_URI=
# Render sets PORT automatically; Express will use it
PORT=5000
# REQUIRED: generate a strong random secret (32+ chars)
JWT_SECRET=
# Deployed client origin (CORS)
CLIENT_ORIGIN=https://nqueens-game.netlify.app
